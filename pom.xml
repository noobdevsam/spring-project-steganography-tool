<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.6</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.example</groupId>
    <artifactId>spring-project-steganography-tool</artifactId>
    <version>v1</version>
    <name>spring-project-steganography-tool</name>
    <description>A powerful and secure steganography tool built with Spring Boot 3</description>
    <url>https://github.com/noobdevsam/spring-project-steganography-tool</url>
    <licenses>
        <license>
            <name>GNU AGPLv3</name>
            <url>https://github.com/noobdevsam/spring-project-steganography-tool/blob/master/LICENSE</url>
        </license>
    </licenses>
    <developers>
        <developer>
            <name>noobdevsam</name>
            <url>https://github.com/noobdevsam</url>
        </developer>
    </developers>
    <scm>
        <connection>scm:git:git://github.com/noobdevsam/spring-project-steganography-tool.git</connection>
        <developerConnection>scm:git:ssh://github.com:noobdevsam/spring-project-steganography-tool.git
        </developerConnection>
        <tag>HEAD</tag>
        <url>https://github.com/noobdevsam/spring-project-steganography-tool</url>
    </scm>
    <properties>
        <java.version>25</java.version>
        <org.mapstruct.version>1.6.3</org.mapstruct.version>
    </properties>
    <dependencies>

        <!--
            If you want docker compose services to auto-start during startup, enable below dependency.
            If you don't want that, leave the below dependency commented and manually start docker compose services from compose file.
        -->

        <!--        <dependency>-->
        <!--            <groupId>org.springframework.boot</groupId>-->
        <!--            <artifactId>spring-boot-docker-compose</artifactId>-->
        <!--            <scope>runtime</scope>-->
        <!--        </dependency>-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-tomcat</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jetty</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${org.mapstruct.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                    <image>
                        <name>noobdevsam/${project.artifactId}:${project.version}</name>
                        <builder>paketobuildpacks/builder-jammy-tiny</builder>
                        <env>
                            <!-- Configure the buildpack to use Java 25 -->
                            <BP_JVM_VERSION>${java.version}</BP_JVM_VERSION>
                            <!-- Run the app with user/group 1000 to match host user permissions on mounted volumes -->
                            <BP_USER_ID>1000</BP_USER_ID>
                            <BP_GROUP_ID>1000</BP_GROUP_ID>
                        </env>
                    </image>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${org.mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!--
             Enable this only if local native image support is needed. Use Liberica NIK JDK for building native image locally.
             -->
            <!--            <plugin>-->
            <!--                <groupId>org.graalvm.buildtools</groupId>-->
            <!--                <artifactId>native-maven-plugin</artifactId>-->
            <!--            </plugin>-->

        </plugins>
    </build>

</project>